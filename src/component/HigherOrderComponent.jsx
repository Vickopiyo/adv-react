import React from 'react'

const HigherOrderComponent = () => {  

    // A HOC is a function that takes a component as its FIRST argument and returns a new component that wraps the given component providing extra capabilities to it  

  return (    
    <div>

    </div>
  )
}

export default HigherOrderComponent